<div class="ma2 ma0-ns center w-100">
    <div class="w-100 ph2 ph0-ns tc tl-ns">
        <div class="pt2 pt3-ns flex flex-wrap justify-center">
            {% for speaker in speakers %}
                <a class="dt w45 h45 no-underline ma3 pa0 shadow-7 link bn br3 b--transparent hide-child cover bg-center" style="background: url({{ speaker.img }})" href="{{ speaker.link }}" alt="{{ speaker.name }}" >
                    <div class="white dtc v-mid w-100 h-100 child bg-black-40 tc f4 pa1">
                        <p class="b">{{ speaker.name }}</p>
                        <p>{{ speaker.talk }}</p>
                    </div>
                </a>
            {% endfor %}
        </div>
    </div>
</div>